import React from 'react'
import { render, screen } from '@testing-library/react'

describe('test', () => {
  it('render a test', () => {
    expect(1).toEqual(1)
  })
  // it('trigger a failure', () => {
  //   expect(2).toEqual(1)
  // })
})
